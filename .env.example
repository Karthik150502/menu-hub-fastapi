SUPABASE_URL=https://<project-ref>.supabase.co
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_JWT_SECRET=
SUPABASE_API_KEY=
SUPABASE_SECRET_KEY=
DATABASE_URL=

# JSON array of allowed frontend origins (CORS). Defaults to
# ["http://localhost:3000", "http://localhost:8081"] if unset — add your
# app's dev/prod origin(s) here, e.g. for Expo/Metro (8081), Vite (5173), etc.
ALLOWED_ORIGINS=["http://localhost:3000", "http://localhost:8081"]